# Отчёт по выполненной задаче 🚀

**Задача:** Покрыть автотестами механизмы защиты от race condition при синхронной записи на слоты и покупке пакетов тренировок (все три сценария из описания).

## Что сдаём в тестирование

- Полное покрытие всех трёх сценариев синхронной записи и покупки пакетов
- Стабильная инфраструктура автотестов с разделением ролей, concurrency и детальными отчётами
- Авторизация для coach и customer с проверкой ролей и сессий
- Создание тренировок и пакетов с динамическими параметрами (даты, время, maxParticipants — всё в `.env`)
- Удобные скрипты запуска: локально (`python run_all_tests.py`) и в Docker с автоматическим открытием Allure (`run_tests_with_allure.ps1`)
- Полностью контейнеризированное окружение для воспроизводимых прогонов

## Выполнено

### Инфраструктура автотестов

- Полностью рабочая, масштабируемая структура проекта
- Разделение ролей через `--role coach` / `--role customer`
- Маркеры `@pytest.mark.coach` и `@pytest.mark.customer` для selective запуска
- Два независимых клиента (`customer1` и `customer2`) для concurrency-тестов
- Общий `APIClient` с автоматическим логгированием всех запросов/ответов (принты + Allure attachments)
- Детальные Allure-отчёты с шагами, payload, response и динамическими параметрами
- Скрипт `run_all_tests.py` для локального запуска всех тестов
- PowerShell-скрипт `run_tests_with_allure.ps1` — сборка, запуск тестов в Docker и автоматическое открытие Allure-отчёта
- Docker + docker-compose с отдельными сервисами (tests, tests-coach, tests-customer, tests-slots)

### Авторизация

- Логин по email (коуч) и по телефону (клиенты)
- Проверка роли после входа (role=4 для coach, role=5 для customer)
- Автоматическая очистка сессий в фикстурах
- Фикстуры: `logged_in_user`, `customer1`, `customer2`

### Создание тренировок и пакетов (коуч)

- Успешное создание групповой тренировки с `maxParticipants=1`
- Успешное создание пакета тренировок (5 сессий, maxParticipants=1)
- Динамические даты в далёком будущем (+200…+400 дней) — отсутствие overlapping
- Полностью конфигурируемые параметры через `.env` (цена, время, длительность, количество мест и т.д.)
- Автоматическая очистка всех удаляемых пакетов перед каждым тестом (фикстура `clean_packages`)

### Синхронная запись на слоты

- Структура `services/slots` (endpoints, payloads, api_slots)
- Тест `test_slot_booking_max_participants_validation` — валидация максимального количества участников
- Тест `test_sync_slot_booking_last_place` — синхронная запись, определение позднего по timestamp, проверка авто-отписки

### Пакеты тренировок (training-packages)

- Структура `services/package` (endpoints, payloads, api_packages)
- Тест `test_sync_package_buy_two_customers_last_place`:
  - Создание пакета с `maxParticipants=1`
  - Одновременная покупка двумя клиентами через `ThreadPoolExecutor`
  - Ожидание авто-отписки (sleep 3 сек)
  - Проверка: только один клиент может успешно сделать refund (должен остаться владелец)
- Тест `test_sync_package_vs_single_booking_last_place`:
  - Создание пакета и выбор целевой тренировки
  - Одновременная покупка: один клиент — поштучно (`/slots`), второй — пакет
  - Проверка обеих веток авто-отписки (refund + повторная запись)

### Docker конфигурация

- Dockerfile на Python 3.12
- docker-compose.yml с сервисами:
  - `tests` — все тесты
  - `tests-coach` — только коуч
  - `tests-customer` — только клиент
  - `tests-slots` — синхронная запись
- Автоматическое монтирование `.env` и `allure-results`

## Найденные баги

- **Критический баг**: При одновременной покупке пакета тренировок двумя клиентами на последнее место — оба успешно покупают и остаются владельцами пакета. Авто-отписка позднего пользователя **не срабатывает** (оба могут сделать refund).
- В смешанном сценарии (пакет + поштучная покупка) защита работает корректно.
- В обычной синхронной записи на слот — валидация maxParticipants работает (второй клиент получает ошибку).

## Команды запуска

### Локальный запуск

```bash
# Все тесты
pytest -v
# Или
python run_all_tests.py

# Отдельные группы
pytest -v -m coach
pytest -v -m customer

# Docker запуск
# Самый удобный способ
.\run_tests_with_allure.ps1

# Вручную
docker compose up tests